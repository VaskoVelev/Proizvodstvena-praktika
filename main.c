#include "stack.h"
#include <stdio.h>

int main() {
    /*Stack *stack = init_stack(5);

    push(stack, 10);
    push(stack, 20);
    push(stack, 30);

    for (int i = stack->top; i >= 0; i--) {
        printf("%lf\n", stack->arr[i]);
    }
    
    double e = pop(stack);
    printf("Popped element: %d\n", e);

    freeStack(stack);*/

    return 0;
}