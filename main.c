#include "stack.h"
#include <stdio.h>

int main() {
    /*Stack *stack = init_stack(5);

    pushNumber(stack, 10);
    pushSymbol(stack, '+'); 
    pushNumber(stack, 20);
    pushSymbol(stack, '-');
    pushNumber(stack, 30);

    for (int i = stack->top; i >= 0; i--) {
        if (stack->arr[i].type == INT) {
            printf("%d\n", stack->arr[i].value.intValue);
        } else {
            printf("%c\n", stack->arr[i].value.charValue);
        }
    }

    Element e = pop(stack);
    if (e.type == INT) {
        printf("Popped element: %d\n", e.value.intValue);
    } else {
        printf("Popped element: %c\n", e.value.charValue);
    }

    freeStack(stack);*/

    return 0;
}